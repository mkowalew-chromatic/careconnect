# Dependencies
node_modules/

# Build output
dist/
storybook-static/
.turbo/
artifacts/
MANIFEST.json

# Test output
coverage/
test-results/
playwright-report/

# Packed tarballs
*.tgz

# Editor / OS
.DS_Store
.idea/
.vscode/

# Local env overrides
.env
.env.local

# Deploy secrets — per-environment config, kept out of the repo
# (only the example template is committed)
deploy/*.env
!deploy/careconnect.env.example
deploy/runtime/

# Local agent notes
CLAUDE.md

# Runtime data (SQLite, uploads)
data/
